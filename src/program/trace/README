Usage:
  trace [OPTIONS] <subcommand>

  -h, --help
                             Print usage information.
  -p, --pid <pid>
                             Select <pid> explicitly.

SUBCOMMANDS

  profile [OPTIONS]

  -d, --duration <seconds>
                             Profile only the next <seconds> of execution.
  -t, --traces
                             Show trace profile.
  -m, --gc
                             Show Memory/GC profile.

Display profiling information for a running Snabb instance.

  inspect [OPTIONS] <id>

  -j, --jdump <path>
                             Use jdump output from <path>.
  -b, --bytecode
                             Print bytecode for trace.
  -i, --ir
                             Print immediate representation (IR) for trace.
  -m, --mcode
                             Print machine code for trace.

Display information about trace by <id>.
